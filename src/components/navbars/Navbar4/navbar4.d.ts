export interface Notification {
    icon: string;
    variant: string;
    text: string;
    time: string;
}

export interface ProfileOption {
    icon: string;
    label: string;
    redirectTo: string;
}
